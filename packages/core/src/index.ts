// Placeholder for the M1 interpreter core (DESIGN.md §6, M1 row).
// Real implementation — registry, cursor, context, channel, cycle guard,
// output result tree — lands in M1 by promoting prototype/ semantics.
export const CORE_PENDING = "M1";
