{
  "name": "jse-external-comparison",
  "version": "0.0.0",
  "private": true,
  "description": "Third-party validators for the external comparison harness. Not a workspace: install here separately so the root install stays free of them.",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json && eslint --config eslint.config.js ."
  },
  "devDependencies": {
    "ata-validator": "^1.27.1",
    "json-schema-library": "^11.6.2"
  }
}
